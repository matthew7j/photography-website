import React from 'react';

import classes from './BackgroundImageContainer.module.css';

const BackgroundImageContainer = props => {
  return (
    <div className = { classes.backgroundImageContainer } />
  );
};

export default BackgroundImageContainer;